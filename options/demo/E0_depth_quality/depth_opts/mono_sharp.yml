manual_seed: 13
aux_data_model: [mono_depth_sharp]

# datasets
datasets:
  test:
    dataroots:
      mono_depth_sharp: ${oc.env:DEMO_DATA_ROOT}/demo-data/mono-depth_sharp
    depth_normalization: &depth_normalization_ptr
      type: nothing
      depth_range: [null, null]
